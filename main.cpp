#include <bits/stdc++.h>
#include <graphics.h>
#include <conio.h>

#define HEIGHT 240
#define WIDTH 320

int main() {
    initgraph(WIDTH, HEIGHT);
    outtext("Hello World!");

    getch();
    closegraph();

    return 0;
}
