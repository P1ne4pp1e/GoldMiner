#include "src/windowCfg.h"

// 全局变量定义与初始化
bool EnableConsole = false;       // 默认值：控制台禁用
double scaleFactor = 3;         // 默认值：缩放因子为3
int targetFPS = 0;               // 默认值：目标帧率为60
bool running = true;              // 默认值：游戏开始时为运行状态
bool consoleDisplayed = false;    // 默认值：控制台不显示
double frameTime = 1;
